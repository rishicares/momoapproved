# Default: Use relative path for both dev and prod
# Dev: Vite proxy forwards to localhost:3000/api
# Prod: Nginx forwards to backend:3000/api
# DO NOT use full URL as it causes CORS issues
VITE_API_ENDPOINT=/api